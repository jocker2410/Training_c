#include <stdoio.h>

char user_input[250]; 
int word_c, input_c; 

int main()
{
	input_c = word_c = 0; 

	printf("Your text please.");
	scanf("%249s", user_input);
	if user_input == "NULL"
	{
		printf("[-] No user input, pease try once more!");
		exit(1);
	}
	while(user_input[i] == '\0')
	{
		if (user_input == ' ' || user_input == '\t') 	
	}
}
